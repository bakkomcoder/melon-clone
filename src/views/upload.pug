extends base

block content 
    if errorMessage
        span=errorMessage 
    form(method="POST")  
        input(placeholder="Song Title", required, type="text", name="songTitle")
        input(placeholder="Artist", required, type="text", name="artist")
        input(placeholder="Youtube URL", required, type="text", name="youtubeUrl")
        input(type="submit", value="등록")