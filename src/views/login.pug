extends base

block content 
    if errorMessage 
        span=errorMessage
    form(method="POST")
        input(placeholder="이름", name="username", type="text", required)
        input(placeholder="비밀번호", name="password", type="password", required)
        input(type="submit", value="로그인")
    div.auth-switch
        span 계정이 없으신가요?
        a(href="/join") 계정을 만드세요 &rarr;